git init

git add

git commit -m ""

git status

git diff

git log

git log --pretty=oneline

git reset --hard (id)

git reflog

git checkout -- (file name)

git reset head (file name)

git remote add (远程库名) (地址，比如：git@github.com:kunleaf/learngit.git)

git push -u (远程库名) main 

git remote rm (远程库名)

git remote -v

git clone (地址，比如：git@github.com:kunleaf/learngit.git)


 